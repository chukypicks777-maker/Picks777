import { LogOut, Layers, BarChart2, Crown } from "lucide-react";
export default function Navbar({ auth, currency, setCurrency, oddsFormat, setOddsFormat, onLogout, onOpenAdmin, onOpenStats, onOpenParlay, parlayCount }) {
  return <header className="border-b border-white/10 bg-[#0a101a] px-4 py-4"><div className="max-w-7xl mx-auto flex flex-wrap items-center justify-between gap-4"><a href="#partidos" className="font-bold tracking-tight text-lg">DEPORTE<span className="text-sky-300">PICKS</span><span className="eyebrow ml-2">VIP</span></a><nav aria-label="Navegación principal" className="flex flex-wrap gap-2"><button className="control" onClick={onOpenStats}><BarChart2 size={16} />Tablas</button><button className="control" onClick={onOpenParlay}><Layers size={16} />Parlay ({parlayCount})</button>{auth.isAdmin && <button className="control text-amber-300" onClick={onOpenAdmin}><Crown size={16} />Owner</button>}</nav><div className="flex flex-wrap items-center gap-2"><select aria-label="Moneda del simulador" value={currency} onChange={e => setCurrency(e.target.value)} className="control">{["USD", "MXN", "EUR", "COP", "ARS"].map(c => <option key={c}>{c}</option>)}</select><select aria-label="Formato de cuotas" value={oddsFormat} onChange={e => setOddsFormat(e.target.value)} className="control"><option value="decimal">Decimal</option><option value="american">Americano</option></select><button className="control" aria-label="Cerrar sesión" onClick={onLogout}><LogOut size={16} /></button></div></div></header>;
}
